-- Con cabal-install 3.0.0.0 ocurre el error: cabal: installdir is not defined. Set it in your cabal config file or use
--installdir=<path>. No instala el paquete.

-- Se intenta implementar el lenguaje de expresiones booleanas con and y not:
<BoolExpr> ::=
    | Val Bool
    | And <BoolExpr> <BoolExpr>
    | Not <BoolExpr>

-- Cuando olvidé llamar a closeNTs, el error fue que algunas variables no estaban en el scope. Fue poco informativo para
darme cuenta que había olvidado llamar a closeNTs.

-- El mensaje de error no mencionó que olvidé la extension NoMonomorphismRestriction.

-- Aunque solo use template Haskell, además de importar AspectAG.TH, necesito importat AspectAG

-- El ejemplo del paper que define la semántica (utilizando la interfaz aplicativa) del add no compila.
-- Para utilizar la interfaz monádica en lugar de la interfaz aplicativa, se necesita
la extensión NoMonomorphismRestriction, pero el error no lo dice.

-- Estaría bueno tener una lista con los operadores disponibles para concatenar reglas, aspectos, etc.

-- Hay varios combinadores de reglas para generar aspectos: syn / syndef / syndefM, etc.

-- Interfaz para agregar símbolos no terminales dentro de una lista en lugar de uno por uno.